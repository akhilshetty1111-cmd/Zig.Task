{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information"
      }
    },
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/zigzag-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 7
        }
      }
    ]
  },

  "ConnectionStrings": {
    "//": "Local dev only - no production data. Password comes from .env / user-secrets; see .env.example.",
    "ZigZagDb": "Host=localhost;Port=5432;Database=zigzag;Username=zigzag;Password=zigzag;Include Error Detail=true"
  },

  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173" ]
  },

  "Jwt": {
    "//": "Local dev only, matching the ConnectionStrings precedent above - a real, randomly generated 512-bit key committed here so `dotnet run` works with no manual setup. Never a production secret; production comes from Key Vault (Phase 14).",
    "Key": "MunERAy1zHIVHjFyo0cOra8Uc4l/UV7EvYpxmMWVnDhvNRoyFF3E11Bc/QparQ7u71kK1dkZm5SuDQqRUAuR7A=="
  }
}
