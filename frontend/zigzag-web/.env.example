# Copy to .env.local and adjust. .env.local is git-ignored.
# Only VITE_-prefixed variables reach the browser bundle - never put a secret here.

VITE_API_BASE_URL=https://localhost:7135/api
VITE_APP_NAME=ZigZag
